apiVersion: v1
kind: Service
metadata:
  name: basic-api-service
  labels:
    app: helloworld
spec:
  type: ClusterIP
  selector:
    app: helloworld
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 5000
