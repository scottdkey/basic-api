apiVersion: v1
kind: Service
metadata:
  name: basic-api-service
  labels:
    app: basic
spec:
  type: ClusterIP
  selector:
    app: basic
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 5000
